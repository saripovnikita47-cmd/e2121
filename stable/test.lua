-- test.lua — проверочный скрипт для лоадера
-- Положи его на сервер как stable/test.lua и запусти в консоли игры: load test

client.color_log(114, 203, 115, "[test] скрипт получен с сервера и выполнен")

-- зелёный квадрат в левом верхнем углу: рисуется каждый кадр,
-- значит скрипт жив и его колбэки работают
client.set_event_callback("paint", function()
    renderer.rectangle(114, 203, 115, 200, 20, 20, 8, 8)
end)
